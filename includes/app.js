// Da Vinci Front-End Challenge Stemwijzer Willem Xu

// Laat alle subjects zien in variable: subjects in parties.js file
subjects.forEach(function(subject){ 
    console.log(subject)
    }
);


// Maak variables
let count = 0
const title = document.getElementById("statementTitle");
const statement = document.getElementById("statementState");
const backButton = document.getElementById("backButton");
const buttonContainer = document.getElementById("")